import { BaseGenerator } from '../base-generator';
import type { HintCommentRule } from '../../nodes';
export declare class HintCommentGenerator extends BaseGenerator {
    /**
     * Converts a hint rule node to a raw string.
     *
     * @param node Hint rule node
     * @returns Raw string
     */
    static generate(node: HintCommentRule): string;
}
