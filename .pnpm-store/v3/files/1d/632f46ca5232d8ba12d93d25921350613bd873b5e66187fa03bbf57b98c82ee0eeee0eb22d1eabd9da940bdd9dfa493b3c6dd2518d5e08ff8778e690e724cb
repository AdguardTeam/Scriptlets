import { BaseGenerator } from '../base-generator';
import { type UboSelector } from '../../nodes';
export declare class UboSelectorGenerator extends BaseGenerator {
    /**
     * Serializes a uBO selector node to a string.
     *
     * @param node UBO selector node
     * @returns Raw string
     */
    static generate(node: UboSelector): string;
}
